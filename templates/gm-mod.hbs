{{!-- GM Moderation - Simplified --}}
<div class="chatzz-mod-container">
    <div class="chatzz-mod-header">
        <h3><i class="fas fa-gavel"></i> Moderation</h3>
        <div class="chatzz-mod-actions">
            <button type="button" class="chatzz-btn-icon" data-action="refresh" title="Refresh">
                <i class="fas fa-sync"></i>
            </button>
        </div>
    </div>

    <div class="chatzz-mod-content">
        {{!-- Private Chats --}}
        <div class="chatzz-mod-section">
            <div class="chatzz-mod-section-header">
                <h4><i class="fas fa-lock"></i> Private ({{totalPrivate}})</h4>
                <button type="button" class="chatzz-btn-small chatzz-btn-danger" data-action="clearAllPrivate">
                    <i class="fas fa-trash"></i> Clear All
                </button>
            </div>
            
            <div class="chatzz-mod-list">
                {{#each privateChats}}
                <div class="chatzz-mod-item" data-conversation-id="{{this.id}}" data-type="private">
                    <div class="chatzz-mod-item-info">
                        <span class="chatzz-mod-item-name">{{this.name}}</span>
                        <span class="chatzz-mod-item-count">{{this.messageCount}} msgs</span>
                    </div>
                    <div class="chatzz-mod-item-actions">
                        <button type="button" class="chatzz-btn-icon" data-action="viewConversation" title="View">
                            <i class="fas fa-eye"></i>
                        </button>
                        <button type="button" class="chatzz-btn-icon" data-action="clearConversation" title="Clear">
                            <i class="fas fa-eraser"></i>
                        </button>
                        <button type="button" class="chatzz-btn-icon chatzz-btn-danger" data-action="deleteConversation" title="Delete">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>
                {{else}}
                <div class="chatzz-empty-state">
                    <p>No private chats</p>
                </div>
                {{/each}}
            </div>
        </div>

        {{!-- Actor Chats --}}
        <div class="chatzz-mod-section">
            <div class="chatzz-mod-section-header">
                <h4><i class="fas fa-mask"></i> Characters ({{totalActors}})</h4>
                <button type="button" class="chatzz-btn-small chatzz-btn-danger" data-action="clearAllActors">
                    <i class="fas fa-trash"></i> Clear All
                </button>
            </div>

            <div class="chatzz-mod-list">
                {{#each actorChats}}
                <div class="chatzz-mod-item" data-conversation-id="{{this.id}}" data-type="actor">
                    <div class="chatzz-mod-item-info">
                        <span class="chatzz-mod-item-name">{{this.name}}</span>
                        <span class="chatzz-mod-item-count">{{this.messageCount}} msgs</span>
                    </div>
                    <div class="chatzz-mod-item-actions">
                        <button type="button" class="chatzz-btn-icon" data-action="viewConversation" title="View">
                            <i class="fas fa-eye"></i>
                        </button>
                        <button type="button" class="chatzz-btn-icon" data-action="clearConversation" title="Clear">
                            <i class="fas fa-eraser"></i>
                        </button>
                        <button type="button" class="chatzz-btn-icon chatzz-btn-danger" data-action="deleteConversation" title="Delete">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>
                {{else}}
                <div class="chatzz-empty-state">
                    <p>No character chats</p>
                </div>
                {{/each}}
            </div>
        </div>

        {{!-- Group Chats --}}
        <div class="chatzz-mod-section">
            <div class="chatzz-mod-section-header">
                <h4><i class="fas fa-users"></i> Groups ({{totalGroups}})</h4>
                <button type="button" class="chatzz-btn-small chatzz-btn-danger" data-action="clearAllGroups">
                    <i class="fas fa-trash"></i> Clear All
                </button>
            </div>
            
            <div class="chatzz-mod-list">
                {{#each groupChats}}
                <div class="chatzz-mod-item" data-conversation-id="{{this.id}}" data-type="group">
                    <div class="chatzz-mod-item-info">
                        <span class="chatzz-mod-item-name">{{this.name}}</span>
                        <span class="chatzz-mod-item-meta">
                            {{this.memberCount}} members · {{this.messageCount}} msgs
                        </span>
                    </div>
                    <div class="chatzz-mod-item-actions">
                        <button type="button" class="chatzz-btn-icon" data-action="viewConversation" title="View">
                            <i class="fas fa-eye"></i>
                        </button>
                        <button type="button" class="chatzz-btn-icon" data-action="clearConversation" title="Clear">
                            <i class="fas fa-eraser"></i>
                        </button>
                        <button type="button" class="chatzz-btn-icon chatzz-btn-danger" data-action="deleteConversation" title="Delete">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>
                {{else}}
                <div class="chatzz-empty-state">
                    <p>No group chats</p>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</div>
