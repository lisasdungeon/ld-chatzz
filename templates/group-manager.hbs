{{!-- Group Manager - Simplified --}}
<div class="chatzz-manager-container">
    <div class="chatzz-manager-header">
        <h3><i class="fas fa-users-cog"></i> Group Manager</h3>
    </div>

    <div class="chatzz-manager-content">
        {{!-- Two Column Layout --}}
        <div class="chatzz-two-column">
            {{!-- Existing Groups --}}
            <div class="chatzz-column">
                <div class="chatzz-column-header">
                    <span>Groups</span>
                    <div class="chatzz-selection-actions">
                        <button type="button" class="chatzz-btn-icon" data-action="deleteSelected" disabled title="Delete">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>
                
                <div class="chatzz-group-list">
                    {{#each groups}}
                    <div class="chatzz-group-item" data-group-id="{{this.id}}">
                        <input type="checkbox" class="chatzz-group-check">
                        <div class="chatzz-group-info">
                            <div class="chatzz-group-name">{{this.name}}</div>
                            <div class="chatzz-group-meta">
                                {{this.memberCount}} members · {{this.messageCount}} msgs
                            </div>
                        </div>
                        <button type="button" class="chatzz-btn-icon" data-action="editGroup" title="Edit">
                            <i class="fas fa-edit"></i>
                        </button>
                        <button type="button" class="chatzz-btn-icon" data-action="openGroup" title="Open">
                            <i class="fas fa-folder-open"></i>
                        </button>
                    </div>
                    {{else}}
                    <div class="chatzz-empty-state">
                        <i class="fas fa-users"></i>
                        <p>No groups</p>
                    </div>
                    {{/each}}
                </div>
            </div>

            {{!-- Create New Group --}}
            <div class="chatzz-column">
                <div class="chatzz-column-header">
                    <span>Create New</span>
                </div>

                <div class="chatzz-form-group">
                    <label>Name</label>
                    <input type="text" name="newGroupName" placeholder="Group name...">
                </div>

                <div class="chatzz-form-group">
                    <label>Members</label>
                    <div class="chatzz-member-list">
                        {{#each users}}
                        <label class="chatzz-member-option">
                            <input type="checkbox" value="{{this.id}}">
                            <span>{{this.name}}</span>
                            {{#if this.isGM}}<span class="chatzz-gm-badge">GM</span>{{/if}}
                            {{#if this.isOnline}}<span class="chatzz-online-dot"></span>{{/if}}
                        </label>
                        {{/each}}
                    </div>
                </div>

                <button type="button" class="chatzz-btn chatzz-btn-primary" data-action="createGroup">
                    <i class="fas fa-plus"></i> Create Group
                </button>
            </div>
        </div>
    </div>
</div>
